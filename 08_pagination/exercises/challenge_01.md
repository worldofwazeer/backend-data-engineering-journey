# Challenge 1: Pagination Math

**Given:**
* Total records = 247
* Limit per page = 50

**Questions:**
1. What are the `skip` values for the first 4 requests?
2. How many pages will contain data?
3. Which request (by skip value) returns an empty list `[]` to break the loop?